<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.keirekipro.infrastructure.repository.user.mapper.UserMapper">

    <select id="selectByEmail" parameterType="string" resultType="com.example.keirekipro.infrastructure.repository.user.dto.UserAuthInfoDto">
        SELECT
            id,
            email,
            password,
            two_factor_auth_enabled
        FROM
            users
        WHERE
            email = #{email}
    </select>

    <insert id="insert">
        INSERT INTO users(
            id,
            email,
            password,
            username,
            created_at,
            updated_at
        )
        VALUES(
            #{id},
            #{email},
            #{password},
            #{username},
            NOW(),
            NOW()
        )
    </insert>

    <select id="selectPasswordById" resultType="java.lang.String">
        SELECT
            password
        FROM
            users
        WHERE
            id = #{id}
    </select>

    <update id="updatePassword">
        UPDATE
            users
        SET
            password = #{password}
        WHERE
            id = #{id}
    </update>

</mapper>
