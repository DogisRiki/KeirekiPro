<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.keirekipro.infrastructure.repository.user.mapper.UserMapper">

    <select id="findByEmail" parameterType="string" resultType="com.example.keirekipro.infrastructure.repository.user.dto.UserAuthInfoDto">
        SELECT
            id,
            email,
            password,
            two_factor_auth_enabled
        FROM
            users
        WHERE
            email = #{email}
    </select>

    <insert id="registerUser">
        INSERT INTO users(
            id,
            email,
            password,
            username,
            created_at,
            updated_at
        )
        VALUES(
            #{id},
            #{email},
            #{password},
            #{username},
            NOW(),
            NOW()
        )
    </insert>

    <select id="findPasswordById" resultType="java.lang.String">
        SELECT
            password
        FROM
            users
        WHERE
            id = #{id}
    </select>

    <update id="changePassword">
        UPDATE
            users
        SET
            password = #{password}
        WHERE
            id = #{id}
    </update>

</mapper>
