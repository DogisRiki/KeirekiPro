spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  cloud:
    aws:
      secretsmanager:
        enabled: false
      s3:
        enabled: false
      ses:
        enabled: false

jwt:
  secret: dummy

aws:
  ses:
    from-address: "no-reply@test.example.com"
  s3:
    bucket-name: "test-bucket"
